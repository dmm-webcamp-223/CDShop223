<div class="container">
	<div class="row">
		<h2>新着順</h2>
	</div>
    	<% @packages.each do |package| %>
    	<div class="row">
      		<%= link_to package_path(packge.id) do%>
        		<%= attachment_image_tag package, disc_image %>
      		<% end %>
            <%= package.disc_stock %>
        	<%= package.artist %>
        	<%= package.title %>
        	<%= package.price %>
    	<% end %>
    </div>
    <%= paginate @packages, class: "paginate" %>
</div>
