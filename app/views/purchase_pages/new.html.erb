<div class="another_add">
    <%= form_for(@ship_address, url: user_cart_purchase_pages_path(current_user.id,current_cart.id)) do |f| %>
        <h4>郵便番号</h4>
             <%= f.number_field :postal_code %>
         <h4>住所</h4>
             <%= f.text_field :shipping_add %>
         <%= f.submit '確定' %>
    <% end %>
</div>